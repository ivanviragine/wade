# Project Knowledge

Shared learnings from AI planning and implementation sessions.
Read this at the start of every session. Add new entries via `wade knowledge add`.

---

## 6e38b3aa | 2026-03-27 | implementation | Issue #233

After migrating AI tool artifacts off main (#233): The managed gitignore (get_managed_gitignore_patterns in installer.py) now includes .claude/settings.json, .cursor/cli.json, and hook config files (.cursor/hooks.json, .gemini/settings.json, .github/hooks/hooks.json). These were not in the managed block before. The wade repo's own .gitignore also needed manual entries for the session files because the managed block is stale (it's regenerated by wade update, which can't be run from worktrees during implementation). When working in worktrees for the wade repo, always expect untracked session files if the managed gitignore block hasn't been updated recently.

---
