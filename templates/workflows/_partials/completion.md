After the authoritative gate succeeds, present the required step-status summary
and the session handles. Offer the recommended exit action first. If the user
chooses to keep editing, repeat every affected closing step before calling the
gate again.
