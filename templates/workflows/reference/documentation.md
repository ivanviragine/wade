# Documentation decision

Compare the complete branch diff with project-facing and developer-facing
documentation. Update every statement made inaccurate by changed behavior,
flags, configuration, architecture, or defaults. If no update is needed, record
the concrete reason in the session summary.
