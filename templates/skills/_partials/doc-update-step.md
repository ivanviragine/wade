Re-read `git diff <base>...HEAD` and update {doc_targets} — plus anything else
it makes wrong. A new flag, renamed option, or changed default all count.

**State the outcome in one line** — the files you updated (e.g. `Docs: README,
AGENTS.md`) or "no doc changes needed" plus a brief reason. Not a paragraph or a
step recap; silence is not a valid result.
