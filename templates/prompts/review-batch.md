# Batch-review operation contract

Review the supplied multi-branch context for integration coherence. This is
always a standalone foreign delegation: no individual session or worktree
binding is privileged. Selected methodology cannot change the operation scope,
input, time budget, or result contract.

{review_budget}
