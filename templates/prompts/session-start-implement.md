Implementation session. Re-read `.wade/session/WORKFLOW.md`; it owns safety,
review, docs, sync, and completion and lists the frozen methodologies. First run
`wade implementation-session check`. Implement PLAN.md, verify, complete the
binding-aware review step, then use `wade implementation-session done`; never
push or create a PR directly.
