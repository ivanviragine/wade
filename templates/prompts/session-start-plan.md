Planning session. Re-read `.wade/session/WORKFLOW.md`; it owns every fixed step
and lists the frozen WORK and REVIEW methodologies. First run
`wade plan-session check`. Follow the fixed output contract. If
`.wade/plans/interactive-session.json` exists, use `plan-session done` to submit
native plan content, run the mapped review, revise, and complete before exiting.
Otherwise return the artifact for parent collection and review. The parent owns
knowledge-vote handoff and task creation. Never approve implementation.
