Planning phase in a detached worktree (no PLAN.md at the root; plans go under the plan directory).
Produce at least one valid PLAN*.md — a conventional-commit title plus a `## Complexity` section — then run `wade plan-session done`; wade turns the plan file(s) into issue(s) once you exit.
